{
  "name": "typing-svg-action",
  "version": "1.0.0",
  "description": "GitHub Action to generate typing SVGs from issue YAML",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "vitest --run"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "yaml": "^2.3.0"
  }
}
